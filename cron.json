{
  "jobs": [
    {
      "command": "* * * * * php artisan schedule:run >> /dev/null 2>&1",
      "size": "S"
    },
    {
      "command": "* * * * * php artisan queue:work --queue='emails,default' --once --tries=1 >> /dev/null 2>&1",
      "size": "S"
    }
  ]
}
