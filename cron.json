{
  "jobs": [
    {
      "command": "*/10 * * * * php artisan schedule:run >> /dev/null 2>&1",
      "size": "S"
    },
    {
      "command": "*/10 * * * * php artisan queue:work --queue='emails,default' --once --tries=1 >> /dev/null 2>&1",
      "size": "S"
    }
  ]
}
